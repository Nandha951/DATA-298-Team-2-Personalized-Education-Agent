# API URL for the backend (if applicable)
VITE_API_URL=http://localhost:4000/api

# Finetuned model server — set these after running: modal deploy modal_server.py
# Modal prints both URLs on deploy.
# Batch endpoint (returns full response when done):
FINETUNED_ASK_URL=https://<workspace>--piab-inference-inferenceserver-ask.modal.run
# Streaming endpoint (tokens appear as generated — much better UX for demo):
FINETUNED_STREAM_URL=https://<workspace>--piab-inference-inferenceserver-ask-stream.modal.run

# Timeout (ms) before falling back to DeepSeek API.
# DeepSeek R1 reasoning can take 60-90s — 120s gives it room.
FINETUNED_TIMEOUT_MS=120000

# LLM Provider API Keys
# Get your Gemini key at: https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API Key (Optional)
VITE_OPENAI_API_KEY=your_openai_api_key_here

# DeepSeek API Key (Optional)
VITE_DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Model Configurations
# Supported OpenAI models: gpt-4o, gpt-4o-mini, gpt-5-nano, etc.
VITE_OPENAI_MODEL=gpt-5-nano

# Supported DeepSeek models: deepseek-chat, deepseek-reasoner
VITE_DEEPSEEK_MODEL=deepseek-chat
